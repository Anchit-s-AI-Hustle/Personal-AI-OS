# =====================================================
# Personal AI OS — environment configuration
# Copy this file to `.env` and fill in real values.
# =====================================================

# --- LLM provider ---
# Pick one: gemini | groq
# - gemini: free tier 1500 req/day on personal Google accounts. Workspace
#   accounts get limit:0 (Google policy). Generate at https://aistudio.google.com/apikey
# - groq:   free tier 14,400 req/day. No Workspace restriction. Generate at
#   https://console.groq.com/keys
LLM_PROVIDER=gemini

# --- Gemini key (used when LLM_PROVIDER=gemini) ---
GEMINI_API_KEY=AIza-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Recommended: gemini-2.0-flash (free tier, fast).
# Alternative: gemini-2.5-flash, gemini-2.5-pro (smarter, paid).
GEMINI_MODEL=gemini-2.0-flash

# --- Groq key (used when LLM_PROVIDER=groq) ---
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# llama-3.3-70b-versatile (Claude-tier quality), llama-3.1-8b-instant (faster, simpler).
GROQ_MODEL=llama-3.3-70b-versatile

# --- Google Sheets ---
# Just the alphanumeric chunk between /d/ and /edit in the sheet URL.
GOOGLE_SHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz0123456789
# Worksheet/tab name where tasks will be appended.
GOOGLE_SHEET_TAB=Tasks

# --- Gmail polling ---
# Seconds between Gmail polls. 300 = 5 minutes.
POLLING_INTERVAL=300
# Standard Gmail search syntax.
GMAIL_QUERY_FILTER=is:unread newer_than:2d -category:promotions -category:social

# --- Meeting / audio capture ---
# Length of each audio chunk before transcription.
AUDIO_CHUNK_MINUTES=2
# 16000 is recommended for Whisper.
AUDIO_SAMPLE_RATE=16000
# Use the system default mic if blank, otherwise the device index from `python -m sounddevice`.
AUDIO_INPUT_DEVICE=
# Set to `false` to disable audio capture entirely (e.g. on a server).
ENABLE_MEETING_CAPTURE=true

# --- Faster-Whisper ---
# tiny | base | small | medium | large-v3
WHISPER_MODEL=base
# cpu | cuda
WHISPER_DEVICE=cpu
# int8 (low RAM) | int8_float16 | float16 | float32
WHISPER_COMPUTE_TYPE=int8
# auto-detects, or set to `en`, `hi`, etc.
WHISPER_LANGUAGE=

# --- Storage paths ---
DATABASE_PATH=./data/personal_ai_os.db
AUDIO_CHUNKS_DIR=./data/audio_chunks
TRANSCRIPTS_DIR=./data/transcripts
LOGS_DIR=./logs

# --- Google OAuth (for Gmail + Sheets, separate from Gemini key) ---
GOOGLE_CREDENTIALS_PATH=./credentials.json
GOOGLE_TOKEN_PATH=./token.json

# Bind OAuth to a specific Google account. If the consent flow ends up
# signed in with anything else (e.g. your personal Gmail), the token is
# discarded so you can't accidentally bind to the wrong account.
EXPECTED_GOOGLE_ACCOUNT=

# Optional: which Chrome profile to open the consent URL in.
# Leave blank to auto-detect the profile signed into EXPECTED_GOOGLE_ACCOUNT.
# Use folder names from %LOCALAPPDATA%\Google\Chrome\User Data\
# (e.g. "Default", "Profile 1", "Profile 2").
OAUTH_CHROME_PROFILE=

# --- Logging ---
# DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# --- Daily summary ---
# Local hour (0-23) at which the daily strategic summary is generated.
DAILY_SUMMARY_HOUR=21
